#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main()
{
	int year = 0;
	printf("请输入年份：");
	scanf("%d\n",&year);
	if (year % 4 == 0 && year % 100 != 0)
		printf("是闰年");
	else if (year % 400 == 0)
		printf("是闰年");
	return 0;
} 
